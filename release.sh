# This will only work on a mac. You cannot publish mac binaries on any other platform...
WIN_CSC_LINK="${SIGNING_KEY}" WIN_CSC_KEY_PASSWORD="${SIGNING_KEY_PASSWORD}" npm run publish -- --mac --win --linux